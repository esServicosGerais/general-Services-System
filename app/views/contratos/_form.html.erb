<%= form_with(model: contrato) do |form| %>
  <% if contrato.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(contrato.errors.count, "error") %> prohibited this contrato from being saved:</h2>

      <ul>
        <% contrato.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :data, style: "display: block" %>
    <%= form.date_field :data %>
  </div>

  <div>
    <%= form.label :duracao, style: "display: block" %>
    <%= form.text_field :duracao %>
  </div>

  <div>
    <%= form.label :valorTotal, style: "display: block" %>
    <%= form.text_field :valorTotal %>

  </div>

  <div>
    <%= form.label :cliente_id, style: "display: block" %>
    <%= collection_select(:contrato, :cliente_id, Cliente.all, :id, :nome) %>
  </div>

  <div>
    <%= form.label :servico_id, style: "display: block" %>
    <%= collection_select(:contrato, :servico_id, Servico.all, :id, :nome) %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
